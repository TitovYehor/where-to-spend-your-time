export type Tag = {
  id: number;
  name: string;
};

export type TagRequest = {
  name: string;
};

export type TagCreateRequest = {
  name: string;
};

export type TagUpdateRequest = {
  name: string;
};